# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                                       App.Index
POST    /entities                               App.GetEntities
POST    /get_entity                             App.GetEntity
POST    /tables                                 App.GetTables
POST    /views                                  App.GetViews
POST    /protect                                App.Protect
POST    /rules                                  App.GetRules
POST    /testdata                               App.TestSelect

POST    /accounts                               UsersCntl.List
POST    /account                                UsersCntl.Get
POST    /account_update                         UsersCntl.Update

POST    /groups                                 GroupCntl.List
POST    /group_update                           GroupCntl.Update
POST    /group                                  GroupCntl.Get
POST    /group_delete                           GroupCntl.Delete

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all
*       /:controller/:action                    :controller.:action
